source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gemspec

gem "sqlite3"
gem "rails"

group :development, :test do
  gem "rubocop", "~> 0.40.0"
  gem "byebug"
  gem "dotenv"
end